{
  "schemaVersion": 2,
  "comment": [
    "The locked version manifest (PLAN section 04). Nothing is fetched as",
    "'latest at install time': this file is compiled into the binary, so a",
    "given Skrog build always installs exactly these components.",
    "",
    "rootfs is keyed by GOARCH (schemaVersion 2, #388). An architecture that",
    "is ABSENT was never built for that engine; an architecture that is",
    "present with an empty sha256 is built but not yet released. Those are",
    "different failures with different answers, so they are different shapes",
    "here -- do not add an empty arm64 entry to an engine that has no arm64",
    "build, because it turns 'this will never exist' into 'this is coming'.",
    "",
    "rootfs.<arch>.sha256 is filled in when a rootfs release is published,",
    "copied from that architecture's .sha256 asset. While it is empty,",
    "`skrog install` refuses and tells the user to pass --rootfs-url and",
    "--rootfs-sha256 explicitly, rather than silently installing something",
    "unverified. Keep these values in step with guest/rootfs/versions.env.",
    "",
    "Older engines stay listed after a bump, with their checksums intact: they",
    "are what `skrog engine list` offers and what `skrog engine rollback`",
    "goes back to. See docs/bumping-upstream.md.",
    "",
    "A published asset's filename is a fact, not text to be renamed. Every",
    "rootfs below was published as hawser-rootfs-*, before the Skrog rename",
    "(#1); a rename pass rewrote those filenames to skrog-rootfs-* and pointed",
    "the manifest at assets that do not exist, which would have 404'd every",
    "install and every rollback. Releases cut from here on are named",
    "skrog-rootfs-*; the ones already published stay exactly as they were.",
    "Same rule as formerRootfsHomes in internal/provision/canonical.go, and the",
    "same reason: these describe bytes that already exist somewhere.",
    "",
    "For the same reason the entries below carry no -amd64 suffix: they were",
    "published before guest/rootfs/arch.sh put the architecture in the name.",
    "Releases cut from rootfs-v29.8.1-2 onward are suffixed on both",
    "architectures."
  ],
  "engines": [
    {
      "version": "29.8.1",
      "default": true,
      "rootfs": {
        "amd64": {
          "url": "https://github.com/wslkit/skrog/releases/download/rootfs-v29.8.1-3/skrog-rootfs-29.8.1-3-amd64.tar.gz",
          "sha256": "f1be49d99b9adab037480f66a9bedc1bca2a0d1a29e278f99adcb8357d18e4ab"
        },
        "arm64": {
          "url": "https://github.com/wslkit/skrog/releases/download/rootfs-v29.8.1-3/skrog-rootfs-29.8.1-3-arm64.tar.gz",
          "sha256": "0a41ce881421d287aacb8b2272af7f6c246558a1c87954c61033c9d34c786b9b"
        }
      },
      "components": {
        "dockerd": "29.8.1",
        "containerd": "2.1.4",
        "runc": "1.3.0",
        "buildkit": "0.27.0",
        "alpine": "3.24.1",
        "nvidia-cdi-hook": "1.20.0"
      }
    },
    {
      "version": "29.8.0",
      "default": false,
      "rootfs": {
        "amd64": {
          "url": "https://github.com/wslkit/skrog/releases/download/rootfs-v29.8.0-2/skrog-rootfs-29.8.0-2.tar.gz",
          "sha256": "a16cd3bc38f0529542a7fc6fba066d944e96302f459595ebf7c5d04035a600af"
        }
      },
      "components": {
        "dockerd": "29.8.0",
        "containerd": "2.1.4",
        "runc": "1.3.0",
        "buildkit": "0.27.0",
        "alpine": "3.24.1",
        "nvidia-cdi-hook": "1.20.0"
      }
    },
    {
      "version": "29.7.2",
      "default": false,
      "rootfs": {
        "amd64": {
          "url": "https://github.com/wslkit/skrog/releases/download/rootfs-v29.7.2-4/hawser-rootfs-29.7.2-4.tar.gz",
          "sha256": "8a9cb099b6fa58e8633c7612cb9b16ce0284e257ec9bf8a4c594844131f370a1"
        }
      },
      "components": {
        "dockerd": "29.7.2",
        "containerd": "2.1.4",
        "runc": "1.3.0",
        "buildkit": "0.27.0",
        "alpine": "3.24.1",
        "nvidia-cdi-hook": "1.20.0"
      }
    }
  ]
}
