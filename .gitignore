# build output
/out/
guest/rootfs/out/
spike/a/dl/
*.exe

# go
/skrog
go.work
go.work.sum
engine-bin/
/agent
/skrogtray
/skrogtray.exe

# The docs site: content/ is staged from docs/ by scripts/build-docs.ps1, the
# rest is Hugo's output and cache.
/site/content/
/site/public/
/site/resources/
/site/.hugo_build.lock

# the linux guest agent, if built out of tree; it lives in the rootfs
skrog-agent

# the e2e workflow writes this on the runner
test/e2e/e2e.log
